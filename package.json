{
  "name": "50instyle",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "bun run --concurrent 'bun --filter apps/web dev' 'bun --filter apps/api dev'",
    "build": "bun run --filter apps/web build && bun run --filter apps/api build",
    "lint": "bun run --filter '*' lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
